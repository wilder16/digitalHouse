// condicional simple
if ( condicion ) {
    // codigo a ejecutar si la condicion es verdadera
}

// condicional con bloque else

if ( condicion ) {
    // codigo a ejecutar si la condicion es verdadera
} else {
    // codigo a ejecutar si la condicion es falsa
}

// condicion con bloque else if 

if ( condicion ) {
    // codigo a ejecutar si la condicion es verdadera
} else if ( condicion2 ) {
    // codigo a ejecutar si la condicion es verdadera
} else {
    // codigo a ejecutar si la condicion es falsa
}